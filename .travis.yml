install: (cd personal-site-frontend && yarn install)
language: node_js
node_js:
  - "stable"
cache:
  yarn: true
  directories:
    - node_modules
script:
  - sh build/build_and_test_all.sh
deploy:
  provider: surge
  project: personal-site-frontend/dist/
  skip_cleanup: true
  on: release
